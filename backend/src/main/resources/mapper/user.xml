<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.recto.dao.UserDao">
	<select id="getUser" parameterType="int" resultType="User">
		select *
		from user
		where user_seq = #{user_seq}
	</select>
	
	<select id="userInfo" parameterType="String" resultType="User">
		select *
		from user
		where user_id like #{user_id}
	</select>

	<insert id="signUp" parameterType="User">
		insert into user(user_id, user_pwd, user_nickname, manager, user_image)
		values(#{user_id}, #{user_pwd}, #{user_nickname}, #{manager}, #{user_image})
	</insert>

	<select id="login" parameterType="User" resultType="int">
		select count(*)
		from user
		where user_id like #{user_id} and user_pwd like #{user_pwd}
	</select>
</mapper>